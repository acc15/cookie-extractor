import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    typography: {
        h1: {
            fontSize: '3rem',
            marginTop: 20
        },
        h2: {
            fontSize: '1.5rem',
            marginTop: 15
        }
    }
});

export default theme;